{
  "name": "eloguard-billing-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply eloguard-billing --local",
    "d1:migrate:remote": "wrangler d1 migrations apply eloguard-billing --remote"
  },
  "devDependencies": {
    "wrangler": "^4.107.1"
  }
}
